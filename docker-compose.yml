version: '3.8'

services:
  web:
    build: .
    network_mode: "host"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - HTTP_PROXY=http://192.168.1.1:7890
      - HTTPS_PROXY=http://192.168.1.1:7890
    restart: unless-stopped
